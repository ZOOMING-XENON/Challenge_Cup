新增库：
pip install cos-python-sdk-v5 （这是为了导入qcloud_cos，这个库不适配现版本python，所以换了种写法。）
boto3
oss2
以上三个是上传云端用到的。

ollama（用不到本地部署大模型的话就把不用装。）
openai
xgboost
stat
以上四个是大模型和机器学习用到的。

pathlib
apscheduler
shutil
cryptography
tkinter 
atexit
以上是数据备份页面其他一些功能用到的。

新增文件：
训练数据：三个csv，只有那个最长的用得到，其他均为中间产物。

机器学习：
expand.py（增加训练数据。）
k-means++.py（聚类分析来给数据打标签，效果不好，已废弃，但舍不得删。）
rule.py（基于规则给数据打标签。）
XGBoostClassifier.py（训练机器学习模型。）
MachineLearning.py（用上面一个文件训练好的模型输出推荐文件的字典dict2。）

大模型：
Deepseek.py（不用本地部署大模型的话就可以扔掉。)
(相比DeepseekAPI文件的程序快一点，因为装的是轻量的模型。）
DeepseekAPI.py（API连接Deepseek大模型。）
（以上两个文件均可生成推荐文件的字典dict1。）

AI：
AI_analysis.py（将以上两个字点dict1、dict2取并集获得最终的文件推荐字典dict。）
（如果用本地部署就把这个文件中的DeepseekAPI改为Deepseek。）

云端：
cloud_config.py（配置云端对象，目前只配置了腾讯云，用的书培账号，保存结果可在书培账号上看。）
cloud_storage.py（配合上面一个文件实现云端存储功能。）

新增路由：
在app.py的末尾的都是，大多都有backup的字眼，容易识别。

新增页面：
backup.html（主页面）
backup_now.html（立即备份页面）
backup_detail.html（备份详情页面）
backup_schedule.html（备份计划页面）

新增文件夹：
在使用本地备份的功能中，其中一个路径会在项目中创建backups文件夹，储存备份文件。

（可能有遗漏，还望海涵。）


